#spring:
#  profiles:
#    active: local
local:
  server:
    port: ${PORT:8080}
server:
  port: ${PORT:8082}
  springdoc:
    api-docs:
      path: /documentation/v3/api-docs
    swagger-ui:
      user-root:
        path: true
    openApi:
      v3:
        path: /documentation/v3/api-docs
#spring:
#  webflux:
#    base-path: /api/v1
# Gitlab base url
gitlab-base-url:
  project: "http://gitlab.localhost.com:9090/api/v4/projects"